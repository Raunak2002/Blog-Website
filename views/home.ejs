<%- include("partials/header") %>

  <h1>Home</h1>
  <p><%= hContent %></p>

  <% composeData.forEach(function (element){ %>
    <h1><%= element.title %></h1>
    <p style="display: inline;"><%= (element.content).slice(0,100) + " ...." %></p><a href="/posts/<%= element._id %>"> Read More</a>
  <% }); %>

<%- include("partials/footer") %>
